- The volume slider completely broke my computer. It caused the volume to go to zero and then switch the numlock on and off a bunch of times after sliding.
- Have the autoclicker show the live mouse position in the UI. Also add an option to toggle on and off a "live look" at what the autoclicker is doing, which would show a small window with a live feed of the screen and a dot where the mouse is clicking (if possible). Then when choosing a point for the coordinates (from where the current mouse is) it has a delay countdown (choosing from 1-10 seconds) before capturing the coordinates so you can move the mouse to the desired location before it captures the coordinates.
- Have the interval selection in the scheduler allow choosing between seconds and minutes for the interval delay.
- Turn the "Quick Notes" into a full notebook. Allow creating multiple notes, saving them, and deleting them similar to Obsidian. Also add a "notebook" tab where you can see all the notes you've created and click on them to edit them. Add an option to export the notes as a text file/md/pdf as well.
- Along with the notebook, add a "journal" tab where you can write daily journal entries, track mood, and log various activities, people, etc (with markdown formatting and tagging which adds a chip to the entry when tagging someone or something from a certain category). Allow the AI Agent to reference the entries in the journal or notebook. Add the ability to set a 4 digit pin to lock the journal and notebook for privacy. with the 4 dot pin input like on phones.
- Add a "Reminders" tab where you can set reminders for yourself with a title, description, date/time, and optional recurring settings. The AI Agent can also reference the reminders and notify you when they are due. Add an option to have the reminders show as notifications on your computer as well. This should create the ability for the app to create toast notifications on the computer for various things like reminders, journal entries, notes, etc. in it's own style for the toast notification that matches the current UI theme.
- Add a "File Manager" tab where you can browse your computer's files and folders, open files, and perform basic file operations like copy, move, delete, etc. The AI Agent can also reference the file manager to access files it needs to complete tasks. Make it follow the UI theme and have a dark mode option as well. Add a search bar to quickly find files and folders. Also add the ability to preview certain file types (like images, text files, PDFs, etc.) within the file manager tab.

## AI Assistant Development Tasks
Plan out a tab in the AI Assistant (Which we will rename from AI Chat) a tab called "Computer Use Agent" and implement a useable computer use agent that utilizes LM Studio (or API key), screenshots, and an option to toggle a "live look" at what the bot is doing. Additionally, create another tab called AgentCLI which is a terminal environment that the AIAgent automates inside of to conduct various tasks around the computer. Create a tab also for AI Settings to add multiple prompts to load and offload, AI model picker, temperature/LLM settings adjustments, knowledge base, etc. Make this an advanced AI workstation for me to access. Make sure it can work on my hardware as well since I can't reliably run very heavy AI models like 30b or 70b ones with my hardware.

- Create a tab for a "Chore Planner" that the AI Assistant can help manage. This would allow you to input various chores or tasks that need to be done around the house, assign them to different days of the week, and set reminders for when they need to be completed. The AI Assistant can also suggest optimal chore schedules based on your preferences and habits. Additionally, it can provide tips and resources for completing certain chores more efficiently. This should be gamified with a points system and rewards for completing chores, and maybe even a leaderboard if you want to compete with family members or roommates on who can complete the most chores. There should be XP, levels, and achievements for completing chores as well to make it more engaging. Give the AI the ability to generate new chores, achievements, and rewards as well to keep it fresh and interesting with an AI icon button in the UI.
- Create a "Browser" tab that allows you to browse the internet within the app. This would be a simple web browser that you can use to access websites without having to leave the app. The AI Assistant can also help you find information on the web, summarize articles, and even automate certain tasks on websites (like filling out forms, scraping data, etc.). This should have a built-in ad blocker and tracking blocker for privacy. Additionally, it can have a "reading mode" that simplifies the layout of articles for easier reading. The browser should also support multiple tabs and have a bookmark manager for saving your favorite websites. There should also be the ability to add certain images, sites, or articles to a tab to save and even save/export images to a "Media" tab in the app for later viewing or use in prompts for the AI Agent. If time allows, integrate a free to use video downloader that when on a youtube video page, a button pops up at the top of the browser tab (near the navigation bar) that allows you to download the video in various resolutions for offline viewing.
- Create a "Media" tab where you can view and manage all the media files (images, videos, audio) that you've saved or imported into the app. This would allow you to easily access and organize your media files in one place. The AI Assistant can also help you find specific media files based on tags, descriptions, or even content (like finding all images with a certain object in them). Additionally, it can provide basic editing tools for images and videos, like cropping, resizing, adding filters, etc. This should also integrate with the AI Agent so you can easily use media files in your prompts or have the AI generate new media based on your preferences.
- Expand error handling and Playwrite testing for all the existing features in the app to ensure stability and reliability. This would involve writing comprehensive tests for each feature, including edge cases and potential failure points. Additionally, implement better error handling throughout the app to gracefully handle any issues that may arise and provide informative feedback to the user. This will help improve the overall user experience and make the app more robust.
- Test all features and make sure the UI is polished and consistent across all tabs. This would involve going through each tab and feature in the app, testing for any bugs or issues, and making sure the design is cohesive and visually appealing and keeping with the theme.
- Add anything I did not mention that would be a good addition to the app that I may have forgotten to mention.